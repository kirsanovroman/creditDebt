# Быстрый старт для локальной разработки

## Шаг 1: Создайте тестового бота

1. Откройте Telegram → [@BotFather](https://t.me/BotFather)
2. Отправьте: `/newbot`
3. Введите имя: `Debt Tracker Test Bot`
4. Введите username: `debt_tracker_test_bot` (или любой другой, заканчивающийся на `bot`)
5. **Скопируйте токен** который выдаст BotFather (формат: `123456789:ABCdef...`)

## Шаг 2: Настройте .env.local

Файл `.env.local` уже создан. Откройте его и укажите токен тестового бота:

```bash
# Откройте в редакторе
nano .env.local
# или
code .env.local
```

Измените строку:
```env
BOT_TOKEN=your_bot_token_here
```

На:
```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz  # Ваш токен от BotFather
```

**Также проверьте настройки БД** (если они отличаются от вашего .env):
- `DB_HOST=localhost`
- `DB_PORT=5432`
- `DB_NAME=debt_bot`
- `DB_USER=postgres` (или ваш пользователь)
- `DB_PASSWORD=your_password_here` (или пусто, если без пароля)

## Шаг 3: Запустите бота

```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Запустите бота
python3 main.py
```

## Готово! 🎉

Теперь бот работает локально с тестовым токеном и не мешает production боту на сервере.

---

## Проверка

После запуска бота:
1. Найдите вашего тестового бота в Telegram (по username, который вы указали)
2. Отправьте `/start`
3. Бот должен ответить и показать меню

---

## Важно

- ✅ Файл `.env.local` в `.gitignore` - не попадёт в репозиторий
- ✅ Production бот на сервере продолжает работать
- ✅ Можно тестировать локально в любое время
- ⚠️  Не коммитьте `.env.local` в git (уже защищено через .gitignore)
