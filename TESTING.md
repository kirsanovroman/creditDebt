# Инструкция по тестированию изменений

## Остановка текущего процесса бота

### Если бот запущен в терминале (передний план)

1. **Найдите процесс:**
   ```bash
   ps aux | grep "python.*main.py" | grep -v grep
   ```

2. **Остановите процесс:**
   - Если бот запущен в текущем терминале: нажмите `Ctrl+C`
   - Если бот запущен в другом терминале: найдите PID и убейте процесс:
     ```bash
     kill <PID>
     # или принудительно, если не останавливается:
     kill -9 <PID>
     ```

### Если бот запущен через systemd/supervisor

```bash
# Для systemd
sudo systemctl stop debtbot

# Для supervisor
sudo supervisorctl stop debtbot
```

## Запуск бота с новыми изменениями

### 1. Убедитесь, что вы в правильной ветке

```bash
cd /Users/r.kirsanov/debt_bot
git branch
# Должна быть активна ветка: feature/remove-invite-expiry
```

Если нужно переключиться:
```bash
git checkout feature/remove-invite-expiry
```

### 2. Проверьте, что изменения применены

```bash
git log --oneline -1
# Должен показать коммит: "Remove invite expiry limit - make invites unlimited"
```

### 3. Убедитесь, что PostgreSQL запущен

```bash
# Проверьте статус PostgreSQL
pg_isready -h localhost -p 5432

# Или проверьте через systemctl (если используете systemd)
sudo systemctl status postgresql
```

### 4. Запустите бота

```bash
# Активируйте виртуальное окружение (если используете)
source venv/bin/activate

# Запустите бота
python main.py
```

## Тестирование функциональности

### Тест 1: Создание приглашения

1. Откройте бота в Telegram
2. Создайте долг (если ещё нет)
3. Нажмите "👥 Пригласить кредитора"
4. **Проверьте:** Сообщение должно показывать "Приглашение действует бессрочно" (без даты истечения)

### Тест 2: Принятие приглашения (сразу)

1. Скопируйте ссылку приглашения
2. Откройте ссылку в Telegram (или отправьте другому пользователю)
3. **Проверьте:** Приглашение должно принять успешно

### Тест 3: Принятие "истёкшего" приглашения

1. Создайте приглашение
2. Подождите несколько секунд (или измените дату в БД для теста)
3. Попробуйте принять приглашение
4. **Проверьте:** Приглашение должно принять успешно (даже если expires_at в прошлом)

### Тест 4: Просмотр долга кредитором

1. После принятия приглашения, кредитор должен видеть долг в списке
2. Кредитор должен иметь доступ к просмотру долга
3. **Проверьте:** Доступ должен быть бессрочным (не ограничен 7 днями)

### Тест 5: Повторное использование приглашения

1. Попробуйте использовать одно и то же приглашение дважды
2. **Проверьте:** Второй раз должно быть сообщение "Приглашение уже использовано"

## Проверка логов

Во время тестирования следите за логами в терминале:

```bash
# Логи должны показывать:
# - Успешное создание приглашения
# - Успешное принятие приглашения
# - Нет ошибок о истёкших приглашениях
```

## Откат изменений (если нужно)

Если что-то пошло не так:

```bash
# Вернитесь в main
git checkout main

# Или отмените последний коммит в текущей ветке
git reset --hard HEAD~1
```

## После успешного тестирования

Если всё работает:

```bash
# Смержите ветку в main
git checkout main
git merge feature/remove-invite-expiry

# Или отправьте ветку в GitHub для review
git push origin feature/remove-invite-expiry
```

